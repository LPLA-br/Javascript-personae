//const readline = require('readline');
//readline.createInterface({input:process.stdin,output:process.stdout});
//readline.question('>', (entrada)=>{ console.log( entrada ); readline.close(); })

const prompt = require('prompt-sync')(); //prompt-sync é uma função
let a = prompt('>');
console.log( a );

